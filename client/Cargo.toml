[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
serde = { version = "1.0.0", features = ["derive"] }
bincode = "1.0.0"
tokio = { version = "1.50.0", features = ["full"] }
raylib = "5.5.1"
